/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.chesscomfiles.com', 'lh3.googleusercontent.com', "www.chess.com"],
    },
}

module.exports = nextConfig
